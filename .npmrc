# Use pnpm for this workspace
# Package manager version is defined in package.json

# Shared dependencies configuration
shamefully-hoist=false
strict-peer-dependencies=false

# Auto install peers
auto-install-peers=true
