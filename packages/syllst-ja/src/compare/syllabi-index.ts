/**
 * Re-export SyllabiIndex from @syllst/compare.
 *
 * @syllst/ja previously had its own copy; it is now upstreamed.
 */

export { SyllabiIndex } from '@syllst/compare';
export type { IndexedNode } from '@syllst/compare';
